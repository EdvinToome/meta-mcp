If `~/.meta-mcp/site-profiles.local.json` is missing, stop and ask the user to run `/meta-mcp-init` first.

Use `skills/meta-ads-consultant/SKILL.md`.

Pull live Meta data first, inspect the creative and landing page second, use official Meta docs via Context7 third, and only then widen to broader research. Separate facts from inference in the final diagnosis.
