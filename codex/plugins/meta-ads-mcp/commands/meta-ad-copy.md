If `~/.meta-mcp/site-profiles.local.json` is missing, stop and ask the user to run `/meta-mcp-init` first.

Use `skills/meta-ad-copy/SKILL.md`.

Resolve the site profile first, inspect the destination page before writing copy when a URL exists, and follow `AD_COPY_GUIDE.md` for the final output shape.
