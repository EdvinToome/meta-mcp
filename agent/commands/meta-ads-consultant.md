If `.claude/meta-marketing-plugin/site-profiles.local.json` is missing, stop and ask the user to create it from `site-profiles.example.json`.

Use `skills/meta-ads-consultant/SKILL.md`.

Pull live Meta data first, inspect the creative and landing page second, and use official Meta docs via Context7 only when the question depends on platform definitions or API constraints. For burn and routine KPI reads, skip docs lookups. Separate facts from inference in the final diagnosis.
